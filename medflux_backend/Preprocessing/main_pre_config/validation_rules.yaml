meta:
  version: 1
  id: validation_rules_global

validation:
  phase_dir_pattern: "phase_[0-9]{2}_[a-z0-9_]+$"
  required_subfolders:
    - config
    - pipeline_workflow
    - connecters
    - core_functions
    - schemas
    - outputs
    - internal_helpers
    - tests
    - docs
    - assets
    - external_helpers
    - common_files
