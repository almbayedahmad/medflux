## PURPOSE: Repository YAML lint configuration to gate policy directory.
## OUTCOME: Enforces baseline YAML hygiene (indentation, sequences) while
## keeping long lines/doc-start flexible for existing policy files.
extends: default

ignore: []

rules:
  document-start: disable
  line-length: disable
  indentation:
    spaces: 2
    indent-sequences: true
  comments:
    min-spaces-from-content: 1
  truthy: disable
