Context and Fields for logging

- Context fields (auto-injected): run_id, phase, flow, hostname, pid, app_version
- Event fields: code, file_id, input_path, output_path, duration_ms

Include these when they help correlate logs and runs.
