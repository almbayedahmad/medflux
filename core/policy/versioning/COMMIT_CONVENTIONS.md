# Commit Message Conventions

Format: <type>(optional scope): <description>

Allowed types: feat, fix, chore, docs, refactor, test.

Example:
- feat(core): add schema validation
- fix(versioning): correct timestamp format
